apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: purgerequests.velocity.sh
spec:
  group: velocity.sh
  names:
    categories: []
    kind: PurgeRequest
    plural: purgerequests
    shortNames:
    - purge
    singular: purgerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for PurgeRequestSpec via `CustomResource`
        properties:
          spec:
            properties:
              estimatedRecords:
                description: Estimated number of records the operator computed at creation time.
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              olderThan:
                description: ISO-8601 cutoff; records older than this in archive will be purged.
                type: string
              reason:
                description: Free-form reason (set by operator from policy).
                nullable: true
                type: string
              schema:
                description: Schema whose archived records are eligible for purge.
                type: string
              version:
                type: string
            required:
            - olderThan
            - schema
            - version
            type: object
          status:
            nullable: true
            properties:
              approved:
                description: Set true when a human approves via `velocity.sh/approved-by` annotation.
                nullable: true
                type: boolean
              approvedBy:
                nullable: true
                type: string
              conditions:
                default: []
                items:
                  description: Common condition shape reused in `*Status.conditions`.
                  properties:
                    lastTransitionTime:
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    reason:
                      nullable: true
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase reported by an operator-managed resource.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Failed
                - Deprecated
                - Rebuilding
                nullable: true
                type: string
              purgedAt:
                nullable: true
                type: string
              purgedRecords:
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: PurgeRequest
        type: object
    served: true
    storage: true
    subresources:
      status: {}
