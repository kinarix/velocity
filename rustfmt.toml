edition              = "2021"
max_width            = 100
use_small_heuristics = "Max"
newline_style        = "Unix"
reorder_imports      = true

# The following are nightly-only; uncomment when CI runs nightly fmt:
# imports_granularity = "Crate"
# group_imports       = "StdExternalCrate"
